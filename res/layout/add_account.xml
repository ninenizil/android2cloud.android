<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:padding="20px">
    <TextView
    	android:id="@+id/description"
    	android:layout_width="fill_parent"
    	android:layout_height="wrap_content"
        android:padding="20dp"
    	android:text="android2cloud allows you to choose where your data is stored. Enter the full email address of an account and the domain of a host (e.g. android2cloud.appspot.com) to the input below to add it to your settings."/>
    <TextView
        android:id="@+id/account_label"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Account:"
        android:layout_below="@+id/description"/>
    <EditText
        android:id="@+id/account_entry"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@android:drawable/editbox_background"
        android:layout_below="@id/account_label"/>
    <TextView
        android:id="@+id/host_label"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Host:"
        android:layout_below="@+id/account_entry"/>
    <EditText
        android:id="@+id/host_entry"
        android:inputType="textUri"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@android:drawable/editbox_background"
        android:layout_below="@id/host_label"/>
    <Button
        android:id="@+id/go"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/host_entry"
        android:layout_alignParentRight="true"
        android:layout_marginLeft="10dip"
        android:text="Save" />
    <Button
    	android:id="@+id/cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toLeftOf="@id/go"
        android:layout_alignTop="@id/go"
        android:text="Cancel" />
</RelativeLayout>